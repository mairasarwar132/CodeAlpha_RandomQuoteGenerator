class AppConstants {
  const AppConstants._();

  static const String appName = 'QuoteFlow';
  static const String appTagline = 'Daily inspiration, beautifully delivered.';
  static const Duration quoteTransitionDuration = Duration(milliseconds: 300);
  static const double maxContentWidth = 760;
}
